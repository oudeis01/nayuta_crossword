# 데이터 아티팩트 탐색 결과

> 작성일: 2026-06-14
> 대상: `data/word_sentences.json`, `data/word_signals.json`, `data/hint_prefill.json`

---

## 1. 단어풀 개요

| 항목 | 값 |
|---|---|
| `word_sentences.json` 전체 단어 | 3,113개 |
│ `general` tier | 2,784개 |
│ `focus` tier | (나머지) |
| `word_signals.json` 전체 단어 | 8,759개 |
| word_sentences에만 있고 word_signals에 없는 단어 | 2,225개 (71.5%) |
│ 이 중 freq < 30 | 615개 |
| `hint_prefill.json` low-confidence | 372개 |
│ 이 중 word_signals 미등재 | 268개 (72%) |
│ 이 중 word_signals 있음 | 104개 |

---

## 2. 발견된 아티팩트 분류

### Type A1: 복합어 분절 (Compound Split)

복합어가 공백으로 분절되어 fragment만 단어로 추출된 케이스.

| 단어 | freq | nf | 문장 패턴 | 근거 |
|---|---|---|---|---|
| **afore** | 1 | 1 | 4/4 문장이 `"afore mentioned"` | "aforementioned"가 PDF 추출 과정에서 공백 분절 → "afore"만 추출. 영단어 아님. |

### Type A2: 고유명사 Fragment

저자명/단체명에서 잘려나온 조각으로, 크로스워드 단어로 무의미.

| 단어 | freq | nf | 출처 | 근거 |
|---|---|---|---|---|
| **trow** | 6 | 3 | `Trow, Within the Context of No-Context, 1981` | 저자 성(姓). George Trow / Martin Trow. 3개 예문이 전부 동일 인용. |
| **spina** | 7 | 2 | `About SpinaguSpinaguis a practice...` | "Spinagu"라는 회사/스튜디오명의 일부. 2개 문장 모두 동일 출처. |

### Type A3: 비영어 단어

영어가 아닌 언어의 단어. 영문 크로스워드에서 사용될 가능성 낮음.

| 단어 | 언어 | freq | 비고 |
|---|---|---|---|
| **reve** | 프랑스어 ("꿈") | 7 | LLM flag = `none_good` (힌트 생성 불가). 10개 문장 중 1개만 clean |
| **casse** | 프랑스어 ("깨뜨리다", "금전등록기") | (저빈도) | |
| **rideau** | 프랑스어 ("커튼") | (저빈도) | |
| **akan** | 인도네시아어/말레이어 ("~할 것이다") | (저빈도) | |
| **egal** | 독일어 ("상관없다") | (저빈도) | |
| **desi** | 힌디어/우르두어 ("현지의") | (저빈도) | 영국 영어에서 통용되나 크로스워드용으로는 난해 |
| **lire** | 프랑스어 ("읽다") / 이탈리아어 (옛 통화) | (저빈도) | 1개 문장에서 `"wild-lire"`로 등장 → `wildfire`의 OCR 오독 가능성 |
| **ultimo** | 라틴어/이탈리아어 ("지난달의") | (저빈도) | |

### Type A4: 철자 오류

| 단어 | freq | 근거 |
|---|---|---|
| **alot** | 43 | "a lot"의 잘못된 표기. 43회 등장, 포함 허용 가능. |

### Type B: Valid Word, 예문만 오염

단어 자체는 유효한 영어 단어이나, 추출된 예문이 특정 패턴으로 오염됨.

| 단어 | freq | in_dict | 오염 패턴 | 오염 비율 |
|---|---|---|---|---|
| **ace** | 180 | True | `"pi ace"` = PDF가 "place"를 잘못 읽음 | 6/10 (60%) |
| **aces** | 34 | False | `"PI aces"` / `"pi aces"` = PDF가 "places"를 잘못 읽음 | 9/10 (90%) |
| **silo** | (저빈도) | (확인중) | `"Th silo"` = "The silo"에서 'e' 빠짐 (OCR typo) | 1문장 |
| **ike** | (저빈도) | - | `"/ike"` = "like"의 '/' + 'l' 빠짐 | (OCR) |

**판단**: Type B 단어는 제거하지 말고, 오염되지 않은 clean 예문만 남기거나 hint를 보강.

### Type C: LLM Deferred / Low-Confidence (But Valid)

LLM이 hint 생성 실패(hard tag / deferred)했으나 단어 자체는 valid English.

| 단어 | freq | nf | LLM tag | 판단 |
|---|---|---|---|---|
| **cretin** | 5 | 3 | hard, defer | valid English (멍청이) |
| **loons** | 5 | 1 | hard, defer | valid English (미친 사람들) |
| **tyke** | 5 | 3 | hard, defer | valid English (꼬마) |
| **edens** | 10 | 2 | hard, defer | valid proper noun (에덴/낙원들) |
| **snog** | 11 | 5 | hard, defer | valid British slang (뽀뽀하다) |
| **tars** | 12 | 3 | hard, defer | valid English (타르, 복수형) |
| **gout** | (저빈도) | - | hard, defer | valid medical term (통풍) |
| **cunts** | (저빈도) | - | flag=offensive | valid but offensive |
| **aye** | (저빈도) | - | - | valid archaic English ("예") |
| **ere** | 11 | 9 | lang=de (자동탐지 오류) | valid English ("~전에", 고어/시어) |

**판단**: 대부분 keep 가능. LLM이 `hard`로 태그한 사유는 주로 (1) 단어가 예문 내에서 자연스럽게 드러나지 않음, (2) 단어의 의미가 예문에서 모호함. 

### Type D: 소프트하이픈(Soft-Hyphen) 코즈메틱

**영향 없음**. 100개 이상의 단어가 문장 텍스트에 `\xad`(soft hyphen)를 포함하나:

- `scan_caps.py`: `SOFT_HYPH_RE = re.compile(r"\xad\s*")`로 제거 후 처리
- `mine_sentences.py`: 토크나이저 `_CASE_TOKEN_RE = re.compile(r"[A-Za-z]+")`가 soft hyphen을 자동 건너뜀

→ 단어 추출 결과에 전혀 영향을 주지 않음. 문장 원문에만 잔재.

**예시**: `tak\xad ing` → `taking`, `per\xad formed` → `performed`, `pro\xad ject` → `project`, `dis\xad tance` → `distance`

---

## 3. 데이터 구조 인사이트

### 3.1 word_signals.json 미등재 2,225개 문제

- `enrich_signals` 파이프라인을 통과하지 못한 단어들
- 이 중 615개는 freq < 30
- 하지만 대다수는 valid English word이며, 단순히 signal enrichment을 안 거친 것뿐
- Type A1~A4(진짜 invalid)는 이 중 극소수(~12개)

**결론**: "signal 없음 = invalid"로 볼 수 없음. freq, in_dict(확인 가능할 경우), 문장 패턴, LLM 평가를 종합해야 함.

### 3.2 LLM 평가 시스템의 편향

`hint_prefill.json` low-confidence 372개 분석:

| 구분 | 개수 |
|---|---|
| tag=hard (힌트 생성은 됐으나 난이도 높음) | 356 |
| defer=true (문장/힌트 없음) | 16 |
| flag=none_good (적절한 hint 없음) | 1 (`reve`) |
| flag=offensive (공격적) | 1 (`cunts`) |

- LLM이 `hard`로 태그한 주요 사유: (1) 단어가 예문에서 모호하게 사용됨, (2) 단어가 고어/방언/은어, (3) 예문이 추상적.
- `hard` 태그는 invalid 판단의 근거가 될 수 없음.

### 3.3 pi-split 패턴 분석

PDF 추출 과정에서 "place"가 "pi ace"로 잘못 읽히는 현상:

```
place → pi ace  (공백 분절 + pi 인식)
places → PI aces / pi aces
```

- `ace`: 6/10 문장이 이 패턴. 그러나 4개의 clean 문장 존재, `in_dict=True`, freq=180 → **keep**
- `aces`: 9/10 문장이 이 패턴. clean 문장 1개. `in_dict=False` → keep 여부 재검토 가능

### 3.4 복합어 분절 탐지 결과

단어가 더 큰 단어의 일부로 등장하는 비율(50% 이상)을 보인 단어:

| 단어 | 분절 비율 | 판단 |
|---|---|---|
| `afore` | 4/4 (100%), 전부 "aforementioned" | **제거** |
| `aqua` | 5/10, "aqua_forensic" 등 | keep, valid word |
| `bod` | 6/10, "Parker's bod" | keep, valid British slang |
| `sens` | 5/10, "Le sens I bis" | keep, valid in context |
| `spina` | 1/2, "Spinagu" | 제거 |

복합어 분절로 인해 실제 invalid한 단어는 `afore` 하나뿐.

---

## 4. 권장 액션 아이템

### 즉시 제거 (Type A1~A4)

```
afore, trow, spina, reve, casse, rideau, akan, desi, lire, ultimo, (alot?)
```

→ 약 12개 단어, `build_wordpool.py`의 stopword/필터 로직에 추가하거나 별도 제거 스크립트.

### 예문 정리 (Type B)

- `ace`: pi-split 포함된 6개 예문 걸러내고 clean 4개만 사용
- `aces`: clean 1개만 있어 hint 재생성 또는 제거 검토
- `silo`: "Th silo" 예문 제거

### 재검토 (Type C)

- `cretin`, `trow`(이미 Type A2), `loons`, `tyke`, `edens`, `snog`, `tars`, `gout` → hint 재생성 시도
- `aye`, `ere` → 크로스워드에서 자주 쓰이는 valid word이므로 유지

### 유지 (Type D 및 기타)

- 소프트하이픈 관련 단어 100+개: 조치 불필요
- word_signals 미등재 2,225개: bulk 재처리(pipeline) 검토

---

## 5. 미해결 질문

1. **Type A3 비영어 단어** 중 일부(`desi`, `egal`)는 특정 영역에서 영어처럼 사용됨. 어디까지를 제거 기준으로 할지?
2. **`aces`** (`in_dict=False`, 90% 오염) clean 문장 1개만으로 유지할 가치가 있는지?
3. **hint 재생성 파이프라인** deferred/hard 단어 372개에 대해 hint를 재생성할지, 아니면 단순 드랍할지?
